# Error Tree

This feature can be accessed through the second tab inside the custom *SecAI* web pages. If you are unsure how to reach this part of the interface, refer to the [this overview](overview.md#custom-web-pages-within-sonarqube).

mouse wheel to zoom, drag to move. control panel??

![Error tree]()

each node is an issue. for info on colours open legend in bottom right. click/hover on it for more info. **View More** opens the detailed error view described [here](./vulnerabilies-list.md). **AIFix** opens the interface to generate an [*AIFix*](./aifix.md).

![Error node tooltip]()

how it's sorted. clicking on a node highlights path. Configuration options at the bottom

![Error path highlighting]()

more options: filter by class, theme